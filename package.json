{
  "name": "dynamica",
  "version": "1.0.0",
  "main": "build/main.js",
  "license": "CC-BY-4.0",
  "private": false,
  "dependencies": {
    "cross-env": "^7.0.0"
  },
  "directories": {
    "buildResources": "build",
    "app": "build"
  },
  "scripts": {
    "build": "gulp && electron-packager ./ Dynamica --platform=win32 --arch=ia32 --overwrite",
    "start": "gulp && electron ."
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "electron": "^10.2.0",
    "electron-builder": "^22.9.1",
    "electron-packager": "^15.1.0",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.3.0",
    "gulp-sourcemaps": "^2.6.5",
    "gulp-typescript": "^6.0.0-alpha.1",
    "typescript": "^4.0.5"
  }
}
